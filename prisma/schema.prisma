// Prisma Schema for ClawHub MySQL Migration
// Based on Convex schema.ts

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// ============================================
// Core Tables - User & Authentication
// ============================================

model users {
  id                        String    @id @default(cuid())
  name                      String?
  image                     String?
  email                     String?   @unique
  emailVerificationTime     DateTime?
  phone                     String?   @unique
  phoneVerificationTime     DateTime?
  isAnonymous               Boolean   @default(false)
  handle                    String?   @unique
  displayName               String?
  bio                       String?
  role                      String?   @default("user") // admin, moderator, user
  githubCreatedAt           DateTime?
  githubFetchedAt           DateTime?
  githubProfileSyncedAt     DateTime?
  trustedPublisher          Boolean   @default(false)
  publishedSkills           Int       @default(0)
  totalStars                Int       @default(0)
  totalDownloads            Int       @default(0)
  personalPublisherId       String?
  requiresModerationAt      DateTime?
  requiresModerationReason  String?
  deactivatedAt             DateTime?
  purgedAt                  DateTime?
  deletedAt                 DateTime?
  banReason                 String?
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt

  // Relations
  ownedSkills               skills[]         @relation("SkillOwner")
  ownedPublishers           publishers[]      @relation("PublisherOwner")
  publishedPackages         packages[]        @relation("PackageOwner")
  comments                  comments[]
  stars                     stars[]
  skillReports              skillReports[]
  skillAppeals              skillAppeals[]
  apiTokens                 apiTokens[]
  publisherMemberships      publisherMembers[]
  createdSkillVersions      skillVersions[]
  createdBadges             skillBadges[]

  @@index([handle])
  @@index([email])
  @@index([phone])
}

// ============================================
// Publisher (User/Org) Tables
// ============================================

model publishers {
  id                  String    @id @default(cuid())
  kind                String    @default("user") // user, org
  handle              String    @unique
  displayName         String
  bio                 String?
  image               String?
  linkedUserId        String?
  trustedPublisher    Boolean   @default(false)
  publishedSkills     Int       @default(0)
  publishedPackages   Int       @default(0)
  totalInstalls       Int       @default(0)
  totalDownloads      Int       @default(0)
  totalStars          Int       @default(0)
  skillTotalInstalls  Int       @default(0)
  skillTotalDownloads Int       @default(0)
  skillTotalStars     Int       @default(0)
  deactivatedAt       DateTime?
  deletedAt           DateTime?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  // Relations
  owner               users?    @relation("PublisherOwner", fields: [linkedUserId], references: [id])
  members             publisherMembers[]
  skills              skills[]
  packages            packages[]
  officialPublishers   officialPublishers[]

  @@index([handle])
  @@index([linkedUserId])
  @@index([kind])
}

model publisherMembers {
  id          String   @id @default(cuid())
  publisherId String
  userId      String
  role        String   @default("publisher") // owner, admin, publisher
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  publisher   publishers @relation(fields: [publisherId], references: [id], onDelete: Cascade)
  user        users      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([publisherId, userId])
  @@index([publisherId])
  @@index([userId])
}

model officialPublishers {
  id           String   @id @default(cuid())
  publisherId  String   @unique
  reason       String?
  createdById  String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  publisher    publishers @relation(fields: [publisherId], references: [id], onDelete: Cascade)

  @@index([publisherId])
}

// ============================================
// Skills Tables
// ============================================

model skills {
  id                       String    @id @default(cuid())
  slug                     String
  displayName              String
  summary                  String?
  icon                     String?
  resourceId               String?
  ownerUserId              String
  ownerPublisherId         String?
  canonicalSkillId         String?
  forkOfSkillId            String?
  forkOfKind               String?   // fork, duplicate
  forkOfVersion            String?
  forkOfAt                 DateTime?
  installKind              String?   // github
  githubSourceId           String?
  githubPath               String?
  githubHasSkillCard       Boolean?
  githubCurrentCommit      String?
  githubCurrentContentHash String?
  githubCurrentStatus      String?   // present, missing, unknown
  githubCurrentCheckedAt   DateTime?
  githubScanStatus         String?   // clean, suspicious, malicious, pending, failed
  githubRemovedAt          DateTime?
  latestVersionId          String?
  latestVersionSummary     Json?
  tags                     Json?
  capabilityTags           Json?     // Array of strings stored as JSON
  softDeletedAt            DateTime?
  moderationStatus         String?   // active, hidden, removed
  moderationNotes          String?
  moderationReason         String?
  moderationVerdict        String?   // clean, suspicious, malicious
  moderationReasonCodes    Json?     // Array of strings stored as JSON
  moderationEvidence       Json?
  moderationSummary        String?
  moderationEngineVersion  String?
  moderationEvaluatedAt    DateTime?
  manualOverride           Json?
  quality                  Json?
  isSuspicious             Boolean?
  moderationFlags          Json?     // Array of strings stored as JSON
  lastReviewedAt           DateTime?
  scanLastCheckedAt        DateTime?
  scanCheckCount           Int?
  hiddenAt                 DateTime?
  hiddenBy                 String?
  unpublishedSlugReservedUntil DateTime?
  unpublishedSlugReleasedAt     DateTime?
  unpublishedOriginalSlug       String?
  reportCount              Int       @default(0)
  lastReportedAt           DateTime?
  batch                    String?
  
  // Stats (canonical fields)
  statsDownloads           Int       @default(0)
  statsStars               Int       @default(0)
  statsInstallsCurrent     Int       @default(0)
  statsInstallsAllTime     Int       @default(0)
  statsVersions            Int       @default(0)
  statsComments            Int       @default(0)
  
  createdAt                DateTime  @default(now())
  updatedAt                DateTime  @updatedAt

  // Relations
  owner                    users      @relation("SkillOwner", fields: [ownerUserId], references: [id])
  publisher                publishers? @relation(fields: [ownerPublisherId], references: [id])
  canonicalSkill           skills?     @relation("SkillCanonical", fields: [canonicalSkillId], references: [id])
  forks                    skills[]    @relation("SkillCanonical")
  versions                 skillVersions[]
  comments                 comments[]
  stars                    stars[]
  skillBadges              skillBadges[]
  skillReports             skillReports[]
  skillAppeals             skillAppeals[]
  skillEmbeddings          skillEmbeddings[]
  dailyStats               skillDailyStats[]
  statEvents               skillStatEvents[]

  @@unique([ownerUserId, slug])
  @@index([ownerPublisherId])
  @@index([slug])
  @@index([statsDownloads])
  @@index([statsStars])
  @@index([statsInstallsAllTime])
  @@index([softDeletedAt])
  @@index([moderationStatus])
}

model skillVersions {
  id                       String    @id @default(cuid())
  skillId                  String
  version                  String
  fingerprint              String?
  sourceProvenance         Json?
  changelog                String    @default("")
  changelogSource          String?   // auto, user
  icon                     String?
  files                    Json      // Array of file objects
  parsed                   Json
  createdBy                String
  createdAt                DateTime  @default(now())
  clawScanNote             String?
  clawScanNoteUpdatedAt    DateTime?
  softDeletedAt            DateTime?
  sha256hash               String?
  vtAnalysis               Json?
  skillSpectorAnalysis     Json?
  llmAnalysis              Json?
  capabilityTags           Json?     // Array of strings stored as JSON
  depRegistryAnalysis      Json?
  depRegistryScanStatus    String?
  staticScan               Json?
  apiKeyRequired           Boolean?

  // Relations
  skill                    skills    @relation(fields: [skillId], references: [id], onDelete: Cascade)
  creator                  users      @relation(fields: [createdBy], references: [id])

  @@unique([skillId, version])
  @@index([skillId])
  @@index([sha256hash])
}

model skillEmbeddings {
  id                 String   @id @default(cuid())
  skillId            String
  versionId          String
  ownerId            String
  ownerPublisherId   String?
  embedding          Json     // Array of floats stored as JSON
  isLatest           Boolean  @default(true)
  isApproved         Boolean  @default(true)
  visibility         String   @default("public")
  updatedAt          DateTime @updatedAt

  // Relations
  skill              skills   @relation(fields: [skillId], references: [id], onDelete: Cascade)

  @@index([skillId])
  @@index([versionId])
}

model skillBadges {
  id       String   @id @default(cuid())
  skillId  String
  kind     String   // highlighted, official, deprecated, redactionApproved
  byUserId String
  at       DateTime @default(now())

  skill    skills   @relation(fields: [skillId], references: [id], onDelete: Cascade)
  user     users    @relation(fields: [byUserId], references: [id])

  @@unique([skillId, kind])
  @@index([skillId])
  @@index([kind])
}

// ============================================
// Comments & Stars
// ============================================

model comments {
  id                   String    @id @default(cuid())
  skillId              String
  userId               String
  body                 String
  reportCount          Int       @default(0)
  lastReportedAt       DateTime?
  scamScanVerdict      String?   // not_scam, likely_scam, certain_scam
  scamScanConfidence   String?   // low, medium, high
  scamScanExplanation  String?
  scamScanEvidence     Json?     // Array of strings stored as JSON
  scamScanModel        String?
  scamScanCheckedAt    DateTime?
  scamBanTriggeredAt   DateTime?
  createdAt            DateTime  @default(now())
  softDeletedAt        DateTime?
  deletedBy            String?

  skill                skills    @relation(fields: [skillId], references: [id], onDelete: Cascade)
  user                 users     @relation(fields: [userId], references: [id])
  reports              commentReports[]

  @@index([skillId])
  @@index([userId])
}

model commentReports {
  id        String   @id @default(cuid())
  commentId String
  skillId   String
  userId    String
  reason    String?
  createdAt DateTime @default(now())

  comment   comments @relation(fields: [commentId], references: [id], onDelete: Cascade)

  @@index([commentId])
  @@index([skillId])
  @@index([userId])
}

model stars {
  id        String   @id @default(cuid())
  skillId   String
  userId    String
  createdAt DateTime @default(now())

  skill     skills   @relation(fields: [skillId], references: [id], onDelete: Cascade)
  user      users    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([skillId, userId])
  @@index([skillId])
  @@index([userId])
}

// ============================================
// Reports & Appeals
// ============================================

model skillReports {
  id            String    @id @default(cuid())
  skillId       String
  skillVersionId String?
  version       String?
  userId        String
  reason        String?
  status        String?    @default("open") // open, confirmed, dismissed, triaged
  triagedAt     DateTime?
  triagedBy     String?
  triageNote    String?
  actionTaken   String?   // none, hide
  createdAt     DateTime  @default(now())

  skill         skills    @relation(fields: [skillId], references: [id], onDelete: Cascade)
  reporter      users     @relation(fields: [userId], references: [id])

  @@index([skillId])
  @@index([userId])
  @@index([status])
}

model skillAppeals {
  id            String    @id @default(cuid())
  skillId       String
  skillVersionId String?
  version       String?
  userId        String
  message       String
  status        String    @default("open") // open, accepted, rejected
  resolvedAt    DateTime?
  resolvedBy    String?
  resolutionNote String?
  actionTaken   String?   // none, restore
  createdAt     DateTime  @default(now())

  skill         skills    @relation(fields: [skillId], references: [id], onDelete: Cascade)
  user          users     @relation(fields: [userId], references: [id])

  @@index([skillId])
  @@index([userId])
  @@index([status])
}

// ============================================
// Packages (OpenClaw)
// ============================================

model packages {
  id                    String    @id @default(cuid())
  name                  String
  normalizedName        String    @unique
  displayName           String
  summary               String?
  ownerUserId           String
  ownerPublisherId      String?
  family                String    @default("skill") // skill, code-plugin, bundle-plugin
  channel               String    @default("community") // official, community, private
  isOfficial            Boolean   @default(false)
  runtimeId             String?
  sourceRepo            String?
  latestReleaseId       String?
  latestVersionSummary  Json?
  tags                  Json?
  capabilityTags        Json?     // Array of strings stored as JSON
  executesCode          Boolean?
  compatibility         Json?
  capabilities          Json?
  verification          Json?
  scanStatus            String    @default("pending") // clean, suspicious, malicious, pending, not-run
  stats                 Json
  reportCount           Int       @default(0)
  lastReportedAt        DateTime?
  softDeletedAt         DateTime?
  softDeletedReason     String?
  softDeletedBy         String?
  softDeletedByRole     String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  owner                 users      @relation("PackageOwner", fields: [ownerUserId], references: [id])
  publisher             publishers? @relation(fields: [ownerPublisherId], references: [id])
  releases              packageReleases[]

  @@index([normalizedName])
  @@index([ownerUserId])
  @@index([ownerPublisherId])
  @@index([family])
  @@index([channel])
}

model packageReleases {
  id                      String    @id @default(cuid())
  packageId               String
  version                 String
  changelog               String    @default("")
  summary                 String?
  distTags                Json?     // Array of strings stored as JSON
  files                   Json
  integritySha256         String
  artifactKind            String?   // legacy-zip, npm-pack
  clawpackStorageId       String?
  clawpackSha256          String?
  clawpackSize            Int?
  clawpackFormat          String?
  npmIntegrity            String?
  npmShasum               String?
  npmTarballName          String?
  npmUnpackedSize         Int?
  npmFileCount            Int?
  extractedPackageJson    Json?
  extractedPluginManifest Json?
  normalizedBundleManifest Json?
  compatibility           Json?
  capabilities            Json?
  runtimeId               String?
  sourceRepo              String?
  verification            Json?
  sha256hash              String?
  vtAnalysis              Json?
  skillSpectorAnalysis     Json?
  llmAnalysis             Json?
  staticScan              Json?
  manualModeration        Json?
  source                  Json?
  createdBy               String
  publishActor            Json
  createdAt               DateTime  @default(now())
  clawScanNote            String?
  clawScanNoteUpdatedAt   DateTime?
  softDeletedAt           DateTime?

  package                 packages  @relation(fields: [packageId], references: [id], onDelete: Cascade)

  @@unique([packageId, version])
  @@index([packageId])
}

// ============================================
// Stats & Events
// ============================================

model skillDailyStats {
  id        String   @id @default(cuid())
  skillId   String
  day       DateTime @db.Date
  downloads Int      @default(0)
  installs  Int      @default(0)
  updatedAt DateTime @updatedAt

  skill     skills   @relation(fields: [skillId], references: [id], onDelete: Cascade)

  @@unique([skillId, day])
  @@index([day])
}

model skillStatEvents {
  id          String    @id @default(cuid())
  skillId     String
  kind        String    // download, star, unstar, comment, uncomment, install_new, etc.
  delta       Json?
  occurredAt  DateTime  @default(now())
  processedAt DateTime?

  skill       skills    @relation(fields: [skillId], references: [id], onDelete: Cascade)

  @@index([processedAt])
  @@index([skillId])
}

model globalStats {
  id                 String   @id @default(cuid())
  key                String   @unique
  activeSkillsCount  Int      @default(0)
  updatedAt          DateTime @updatedAt
}

// ============================================
// API Tokens
// ============================================

model apiTokens {
  id          String    @id @default(cuid())
  userId      String
  label       String
  prefix      String
  tokenHash   String    @unique
  createdAt   DateTime  @default(now())
  lastUsedAt  DateTime?
  revokedAt   DateTime?

  user        users     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

// ============================================
// Rate Limiting
// ============================================

model rateLimits {
  id          String   @id @default(cuid())
  key         String
  windowStart DateTime
  shard       Int?
  count       Int      @default(0)
  limit       Int
  updatedAt   DateTime @updatedAt

  @@unique([key, windowStart])
  @@index([key])
}

// ============================================
// Reserved Slugs & Handles
// ============================================

model reservedSlugs {
  id                  String   @id @default(cuid())
  slug                String   @unique
  originalOwnerUserId String
  deletedAt           DateTime @default(now())
  expiresAt           DateTime
  reason              String?
  releasedAt          DateTime?

  @@index([slug])
  @@index([expiresAt])
}

model reservedHandles {
  id                  String   @id @default(cuid())
  handle              String   @unique
  rightfulOwnerUserId String
  reason              String?
  releasedAt          DateTime?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([handle])
}

// ============================================
// Audit Logs
// ============================================

model auditLogs {
  id          String   @id @default(cuid())
  actorUserId String?
  action      String
  targetType  String
  targetId    String
  metadata    Json?
  createdAt   DateTime @default(now())

  @@index([actorUserId])
  @@index([targetType, targetId])
}

// ============================================
// AI Direct Hiring — MySQL source of truth
// ============================================

// P0: Model Catalog, Agents, Versions, Run Audits, Credentials, Organizations
model aiDirectModelCatalog {
  id               String   @id @db.VarChar(36)
  modelKey         String   @unique @db.VarChar(255)
  providerKey      String?  @db.VarChar(64)
  providerModelKey String?  @db.VarChar(255)
  displayName      String   @db.VarChar(255)
  status           String   @default("draft") @db.VarChar(32)
  capabilities     Json?
  taskProfile      Json?
  pricing          Json?
  evidenceVersion  String?  @db.VarChar(128)
  evidence         Json?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@unique([providerKey, providerModelKey])
  @@index([providerKey, status])
  @@index([status])
  @@map("ai_direct_model_catalog")
}

model aiDirectAgents {
  id               String   @id @db.VarChar(36)
  ownerUserId      String   @db.VarChar(191)
  ownerPublisherId String?  @db.VarChar(191)
  name             String   @db.VarChar(120)
  description      String?  @db.Text
  status           String   @default("draft") @db.VarChar(32)
  activeVersionId  String?  @db.VarChar(36)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  appearanceProfile aiDirectAgentAppearanceProfiles?
  appearanceAssets  aiDirectAgentAppearanceAssets[]

  @@index([ownerUserId])
  @@index([ownerPublisherId])
  @@map("ai_direct_agents")
}

model aiDirectAgentVersions {
  id              String   @id @db.VarChar(36)
  agentId         String   @db.VarChar(36)
  version         Int
  status          String   @default("draft") @db.VarChar(32)
  promptSpec      Json
  modelPolicy     Json
  executionPolicy Json
  createdByUserId String   @db.VarChar(191)
  publishedAt     DateTime?
  createdAt       DateTime @default(now())

  @@unique([agentId, version])
  @@index([agentId])
  @@map("ai_direct_agent_versions")
}

model aiDirectAgentPrices {
  id              String    @id @db.VarChar(36)
  agentId         String    @db.VarChar(36)
  agentVersionId  String    @db.VarChar(36)
  developerUserId String    @db.VarChar(191)
  version         Int
  currency        String    @default("CNY") @db.Char(3)
  amountFen       BigInt    @db.UnsignedBigInt
  status          String    @default("active") @db.VarChar(32)
  createdByUserId String    @db.VarChar(191)
  effectiveAt     DateTime  @default(now())
  supersededAt    DateTime?
  createdAt       DateTime  @default(now())

  @@unique([agentId, version], map: "ai_direct_agent_prices_agent_version_key")
  @@index([agentId, status, version], map: "ai_direct_agent_prices_agent_status_version_idx")
  @@index([developerUserId, status], map: "ai_direct_agent_prices_developer_status_idx")
  @@map("ai_direct_agent_prices")
}

model aiDirectModelRunAudits {
  id                String   @id @db.VarChar(36)
  runId             String?  @db.VarChar(36)
  stepId            String?  @db.VarChar(36)
  agentId           String   @db.VarChar(36)
  agentVersionId    String   @db.VarChar(36)
  catalogModelId    String   @db.VarChar(36)
  modelKey          String   @db.VarChar(255)
  providerKey       String?  @db.VarChar(64)
  credentialVersion Int?
  providerRequestId String?  @db.VarChar(191)
  attempt           Int      @default(1)
  taskType          String?  @db.VarChar(128)
  status            String   @db.VarChar(32)
  failureCode       String?  @db.VarChar(128)
  failureClass      String?  @db.VarChar(64)
  inputTokens       Int?
  outputTokens      Int?
  costMicros        BigInt?
  latencyMs         Int?
  routingMetadata   Json
  createdAt         DateTime @default(now())

  @@unique([stepId, attempt])
  @@index([runId, createdAt])
  @@index([runId, createdAt, id], map: "ai_direct_model_audit_run_time_cursor_idx")
  @@index([stepId, createdAt])
  @@index([providerKey, createdAt])
  @@index([agentId, createdAt])
  @@map("ai_direct_model_run_audits")
}

model aiDirectUserCredentials {
  id                 String    @id @db.VarChar(36)
  userId             String    @db.VarChar(191)
  provider           String    @default("jinsha-token") @db.VarChar(64)
  label              String    @default("金沙") @db.VarChar(160)
  fingerprint        String?   @db.Char(64)
  cipherText         String    @db.MediumText
  iv                 String    @db.VarChar(64)
  authTag            String    @db.VarChar(64)
  keyVersion         String    @default("v1") @db.VarChar(32)
  credentialVersion  Int       @default(1)
  validationStatus   String    @default("unvalidated") @db.VarChar(32)
  validatedAt        DateTime?
  lastUsedAt         DateTime?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  revokedAt          DateTime?

  @@unique([userId, provider])
  @@index([validationStatus, updatedAt])
  @@index([userId])
  @@map("ai_direct_user_credentials")
}

model aiDirectAuthIdentities {
  id                  String   @id @db.VarChar(36)
  issuer              String   @db.VarChar(255)
  subject             String   @db.VarChar(191)
  userId              String   @unique @db.VarChar(191)
  lastAuthenticatedAt DateTime @default(now())
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@unique([issuer, subject])
  @@index([lastAuthenticatedAt])
  @@map("ai_direct_auth_identities")
}

model aiDirectOrganizations {
  id                     String   @id @db.VarChar(36)
  name                   String   @db.VarChar(160)
  slug                   String   @unique @db.VarChar(160)
  ownerUserId            String   @db.VarChar(191)
  idempotencyKey         String?  @db.VarChar(128)
  idempotencyFingerprint String?  @db.Char(64)
  status                 String   @default("active") @db.VarChar(32)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  @@unique([ownerUserId, idempotencyKey])
  @@index([ownerUserId])
  @@index([status, updatedAt])
  @@map("ai_direct_organizations")
}

model aiDirectOrganizationMembers {
  id              String   @id @db.VarChar(36)
  organizationId  String   @db.VarChar(36)
  userId          String   @db.VarChar(191)
  role            String   @default("member") @db.VarChar(32)
  status          String   @default("active") @db.VarChar(32)
  createdByUserId String   @db.VarChar(191)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([organizationId, userId])
  @@index([userId, status])
  @@index([organizationId, status])
  @@map("ai_direct_organization_members")
}

model aiDirectAuditEvents {
  id             String   @id @db.VarChar(36)
  organizationId String?  @db.VarChar(36)
  actorUserId    String?  @db.VarChar(191)
  action         String   @db.VarChar(128)
  targetType     String   @db.VarChar(64)
  targetId       String   @db.VarChar(191)
  requestId      String?  @db.VarChar(128)
  outcome        String   @db.VarChar(32)
  metadata       Json?
  createdAt      DateTime @default(now())

  @@index([organizationId, createdAt])
  @@index([organizationId, createdAt, id], map: "ai_direct_audit_org_time_cursor_idx")
  @@index([organizationId, actorUserId, createdAt, id], map: "ai_direct_audit_org_actor_time_cursor_idx")
  @@index([organizationId, targetType, targetId, createdAt, id], map: "ai_direct_audit_org_resource_time_cursor_idx")
  @@index([organizationId, action, createdAt, id], map: "ai_direct_audit_org_action_time_cursor_idx")
  @@index([organizationId, requestId, createdAt, id], map: "ai_direct_audit_org_request_time_cursor_idx")
  @@index([targetType, targetId, createdAt])
  @@index([actorUserId, createdAt])
  @@map("ai_direct_audit_events")
}

model aiDirectAuditExportJobs {
  id                   String    @id @db.VarChar(36)
  organizationId       String    @db.VarChar(36)
  requestedByUserId    String    @db.VarChar(191)
  status               String    @default("queued") @db.VarChar(32)
  filters              Json
  watermark            String    @db.VarChar(512)
  requestId            String?   @db.VarChar(128)
  attemptCount         Int       @default(0)
  leaseOwner           String?   @db.VarChar(128)
  leaseExpiresAt       DateTime?
  artifact             Bytes?    @db.LongBlob
  artifactMimeType     String?   @db.VarChar(255)
  artifactFileName     String?   @db.VarChar(255)
  artifactSizeBytes    BigInt?   @db.UnsignedBigInt
  artifactSha256       String?   @db.Char(64)
  failureCode          String?   @db.VarChar(128)
  startedAt            DateTime?
  completedAt          DateTime?
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt

  @@index([organizationId, createdAt, id], map: "ai_direct_audit_exports_org_created_idx")
  @@index([organizationId, requestedByUserId, createdAt, id], map: "ai_direct_audit_exports_org_requester_created_idx")
  @@index([status, leaseExpiresAt, createdAt, id], map: "ai_direct_audit_exports_queue_idx")
  @@index([organizationId, requestId], map: "ai_direct_audit_exports_request_idx")
  @@map("ai_direct_audit_export_jobs")
}

model aiDirectAuditExportDownloadTokens {
  id               String    @id @db.VarChar(36)
  exportJobId      String    @db.VarChar(36)
  organizationId   String    @db.VarChar(36)
  tokenPrefix      String    @db.VarChar(12)
  tokenHash        String    @unique(map: "ai_direct_audit_download_token_hash_key") @db.Char(64)
  issuedToUserId   String    @db.VarChar(191)
  expiresAt        DateTime
  usedAt           DateTime?
  createdAt        DateTime  @default(now())

  @@index([exportJobId, expiresAt, usedAt], map: "ai_direct_audit_download_job_expiry_idx")
  @@index([organizationId, issuedToUserId, createdAt], map: "ai_direct_audit_download_org_user_created_idx")
  @@map("ai_direct_audit_export_download_tokens")
}

model aiDirectMemoryBindings {
  id               String    @id @db.VarChar(36)
  userId           String    @db.VarChar(191)
  vaultFingerprint String    @db.Char(64)
  status           String    @default("active") @db.VarChar(32)
  extractorVersion String    @db.VarChar(64)
  evidenceVersion  String    @db.VarChar(64)
  noteCount        Int       @default(0)
  tagCount         Int       @default(0)
  lastSyncAt       DateTime?
  revokedAt        DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  @@unique([userId, vaultFingerprint])
  @@index([userId, status])
  @@index([vaultFingerprint])
  @@map("ai_direct_memory_bindings")
}

model aiDirectMemoryDigests {
  id               String    @id @db.VarChar(36)
  bindingId        String    @db.VarChar(36)
  userId           String    @db.VarChar(191)
  vaultFingerprint String    @db.Char(64)
  notePath         String    @db.VarChar(1024)
  noteHash         String    @db.Char(64)
  title            String?   @db.VarChar(512)
  tagsJson         Json?
  linksJson        Json?
  summaryMd        String?   @db.Text
  summaryBytes     Int       @default(0)
  sourceBytes      Int       @default(0)
  redactedAt       DateTime?
  redactionReason  String?   @db.VarChar(128)
  frontmatterJson  Json?
  mtime            DateTime?
  size             Int       @default(0)
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  @@unique([bindingId, notePath(length: 255)])
  @@index([userId, vaultFingerprint])
  @@index([bindingId])
  @@map("ai_direct_memory_digests")
}

model aiDirectOutboxEvents {
  id             String    @id @db.VarChar(36)
  organizationId String?   @db.VarChar(36)
  aggregateType  String    @db.VarChar(64)
  aggregateId    String    @db.VarChar(191)
  eventType      String    @db.VarChar(128)
  payloadVersion Int       @default(1)
  payload        Json
  occurredAt     DateTime  @default(now())
  status         String    @default("pending") @db.VarChar(32)
  attempts       Int       @default(0)
  availableAt    DateTime  @default(now())
  publishedAt    DateTime?
  failedAt       DateTime?
  failureReason  String?   @db.Text

  @@index([status, availableAt])
  @@index([publishedAt, occurredAt])
  @@index([aggregateType, aggregateId, occurredAt])
  @@map("ai_direct_outbox_events")
}

model aiDirectWorkerTokens {
  id              String    @id @db.VarChar(36)
  organizationId  String    @db.VarChar(36)
  workerId        String    @db.VarChar(128)
  name            String    @db.VarChar(160)
  tokenPrefix     String    @db.VarChar(16)
  tokenHash       String    @unique @db.Char(64)
  status          String    @default("active") @db.VarChar(32)
  expiresAt       DateTime?
  lastUsedAt      DateTime?
  revokedAt       DateTime?
  createdByUserId String    @db.VarChar(191)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([organizationId, status])
  @@index([workerId, status])
  @@map("ai_direct_worker_tokens")
}

model aiDirectArtifacts {
  id                String   @id @db.VarChar(36)
  organizationId    String   @db.VarChar(36)
  runId              String   @db.VarChar(36)
  stepId             String?  @db.VarChar(36)
  kind               String   @db.VarChar(64)
  storagePath        String   @db.VarChar(1024)
  storagePathHash    String   @db.Char(64)
  mimeType           String   @db.VarChar(255)
  sizeBytes          BigInt
  sha256             String   @db.Char(64)
  visibility         String   @default("organization") @db.VarChar(32)
  createdByWorkerId  String   @db.VarChar(128)
  createdAt          DateTime @default(now())

  @@unique([runId, storagePathHash])
  @@index([organizationId, createdAt])
  @@index([runId, createdAt])
  @@index([stepId])
  @@map("ai_direct_artifacts")
}

model aiDirectRuntimeMetrics {
  metricKey   String   @id @db.VarChar(128)
  metricValue BigInt   @default(0)
  updatedAt   DateTime @updatedAt

  @@map("ai_direct_runtime_metrics")
}

// P1: Companies, Projects, AgentRoles, CapabilityGrants, Offers, Employments, Approvals, WorkflowRuns
model aiDirectCompanies {
  id               String   @id @default(cuid())
  organizationId   String   @db.VarChar(36)
  name             String   @db.VarChar(160)
  slug             String   @unique @db.VarChar(160)
  status           String   @default("active") @db.VarChar(32)
  createdByUserId  String   @db.VarChar(191)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([organizationId, status])
  @@index([organizationId, updatedAt])
  @@map("ai_direct_companies")
}

model aiDirectCompanyMembers {
  id         String   @id @default(cuid()) @db.VarChar(36)
  companyId  String   @db.VarChar(36)
  userId     String   @db.VarChar(191)
  role       String   @db.VarChar(32)
  status     String   @default("active") @db.VarChar(32)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([companyId, userId])
  @@index([userId, status])
  @@index([companyId, status, role])
  @@map("ai_direct_company_members")
}

model aiDirectProjects {
  id                 String   @id @default(cuid())
  companyId          String   @db.VarChar(36)
  name               String   @db.VarChar(160)
  slug               String   @db.VarChar(160)
  status             String   @default("active") @db.VarChar(32)
  budgetMicros       BigInt?  @default(0)
  sensitivityLimit   String?  @db.VarChar(64)
  createdByUserId    String   @db.VarChar(191)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@unique([companyId, slug])
  @@index([companyId, status, updatedAt])
  @@map("ai_direct_projects")
}

model aiDirectAgentRoles {
  id                    String   @id @default(cuid())
  companyId             String   @db.VarChar(36)
  projectId             String?  @db.VarChar(36)
  name                  String   @db.VarChar(160)
  responsibilities      Json
  requiredCapabilities  Json
  budgetMicros          BigInt?  @default(0)
  status                String   @default("open") @db.VarChar(32)
  createdByUserId       String   @db.VarChar(191)
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([companyId, status, updatedAt])
  @@index([projectId, status])
  @@map("ai_direct_agent_roles")
}

model aiDirectCapabilityGrants {
  id               String    @id @default(cuid())
  subjectType      String    @db.VarChar(32)
  subjectId        String    @db.VarChar(191)
  resourceType     String    @db.VarChar(64)
  resourceId       String    @db.VarChar(191)
  action           String    @db.VarChar(64)
  scope            Json?
  issuedByUserId   String    @db.VarChar(191)
  issuedAt         DateTime  @default(now())
  expiresAt        DateTime?
  revokedAt        DateTime?
  revokedByUserId  String?   @db.VarChar(191)
  revokeReason     String?   @db.Text

  @@index([subjectType, subjectId, resourceType, resourceId, revokedAt])
  @@index([subjectType, subjectId, resourceType, resourceId, action, revokedAt, expiresAt], map: "ai_direct_capability_audit_permission_idx")
  @@index([resourceType, resourceId, revokedAt])
  @@index([revokedAt])
  @@map("ai_direct_capability_grants")
}

model aiDirectHiringIntents {
  id                     String   @id @db.VarChar(36)
  organizationId         String   @db.VarChar(36)
  companyId              String   @db.VarChar(36)
  projectId              String?  @db.VarChar(36)
  roleId                 String   @db.VarChar(36)
  positionId             String   @db.VarChar(36)
  agentId                String   @db.VarChar(36)
  agentVersionId         String   @db.VarChar(36)
  priceId                String   @db.VarChar(36)
  requestedByUserId      String   @db.VarChar(191)
  status                 String   @default("awaiting_payment") @db.VarChar(32)
  approvalId             String?  @db.VarChar(36)
  idempotencyKey         String   @db.VarChar(128)
  idempotencyFingerprint String   @db.Char(64)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  @@unique([requestedByUserId, idempotencyKey], map: "ai_direct_hiring_intents_requester_idempotency_key")
  @@index([companyId, status, createdAt], map: "ai_direct_hiring_intents_company_status_created_idx")
  @@index([positionId, status], map: "ai_direct_hiring_intents_position_status_idx")
  @@index([approvalId], map: "ai_direct_hiring_intents_approval_idx")
  @@map("ai_direct_hiring_intents")
}

model aiDirectPaymentOrders {
  id                  String    @id @db.VarChar(36)
  outTradeNo          String    @unique(map: "ai_direct_payment_orders_out_trade_no_key") @db.VarChar(64)
  hiringIntentId      String    @unique(map: "ai_direct_payment_orders_hiring_intent_key") @db.VarChar(36)
  provider            String    @default("alipay") @db.VarChar(32)
  currency            String    @default("CNY") @db.Char(3)
  grossAmountFen      BigInt    @db.UnsignedBigInt
  platformFeeFen      BigInt    @db.UnsignedBigInt
  developerPayableFen BigInt    @db.UnsignedBigInt
  developerUserId     String    @db.VarChar(191)
  priceId             String    @db.VarChar(36)
  priceVersion        Int
  status              String    @default("pending") @db.VarChar(32)
  providerTradeNo     String?   @db.VarChar(128)
  rawNotifySha256     String?   @db.Char(64)
  paidAt              DateTime?
  fulfilledAt         DateTime?
  offerId             String?   @db.VarChar(36)
  employmentId        String?   @db.VarChar(36)
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  @@unique([provider, providerTradeNo], map: "ai_direct_payment_orders_provider_trade_no_key")
  @@index([status, updatedAt], map: "ai_direct_payment_orders_status_updated_idx")
  @@index([developerUserId, status, updatedAt], map: "ai_direct_payment_orders_developer_status_idx")
  @@map("ai_direct_payment_orders")
}

model aiDirectRevenueLedgerEntries {
  id                 String   @id @db.VarChar(36)
  entryKey           String   @unique(map: "ai_direct_revenue_ledger_entry_key") @db.VarChar(191)
  paymentOrderId     String   @db.VarChar(36)
  accountType        String   @db.VarChar(32)
  accountOwnerUserId String?  @db.VarChar(191)
  direction          String   @default("credit") @db.VarChar(16)
  currency           String   @default("CNY") @db.Char(3)
  amountFen          BigInt   @db.UnsignedBigInt
  status             String   @default("posted") @db.VarChar(32)
  metadata           Json?
  createdAt          DateTime @default(now())

  @@index([paymentOrderId, createdAt], map: "ai_direct_revenue_ledger_order_idx")
  @@index([accountOwnerUserId, accountType, status, createdAt], map: "ai_direct_revenue_ledger_owner_status_idx")
  @@map("ai_direct_revenue_ledger_entries")
}

model aiDirectDeveloperSettlements {
  id                   String    @id @db.VarChar(36)
  developerUserId      String    @db.VarChar(191)
  currency             String    @default("CNY") @db.Char(3)
  amountFen            BigInt    @db.UnsignedBigInt
  status               String    @default("pending") @db.VarChar(32)
  externalReference    String?   @db.VarChar(191)
  createdByUserId      String    @db.VarChar(191)
  completedByUserId    String?   @db.VarChar(191)
  completedAt          DateTime?
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt

  @@index([developerUserId, status, createdAt], map: "ai_direct_developer_settlements_developer_status_idx")
  @@map("ai_direct_developer_settlements")
}

model aiDirectDeveloperSettlementItems {
  settlementId String   @db.VarChar(36)
  ledgerEntryId String  @unique(map: "ai_direct_settlement_items_ledger_key") @db.VarChar(36)
  amountFen    BigInt   @db.UnsignedBigInt
  createdAt    DateTime @default(now())

  @@id([settlementId, ledgerEntryId])
  @@index([settlementId], map: "ai_direct_settlement_items_settlement_idx")
  @@map("ai_direct_developer_settlement_items")
}

model aiDirectOffers {
  id               String    @id @default(cuid())
  roleId           String    @db.VarChar(36)
  agentVersionId   String    @db.VarChar(36)
  companyId        String    @db.VarChar(36)
  projectId        String?   @db.VarChar(36)
  status           String    @default("draft") @db.VarChar(32)
  terms            Json
  approvalId       String?   @db.VarChar(36)
  proposedByUserId String    @db.VarChar(191)
  proposedAt       DateTime  @default(now())
  expiresAt        DateTime?
  acceptedAt       DateTime?
  rejectedAt       DateTime?
  rejectedReason   String?   @db.Text
  paymentOrderId   String?   @unique(map: "ai_direct_offers_payment_order_key") @db.VarChar(36)
  issuedAt         DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  @@unique([roleId, agentVersionId, status])
  @@index([companyId, status, updatedAt])
  @@index([agentVersionId])
  @@map("ai_direct_offers")
}

model aiDirectEmployments {
  id               String    @id @default(cuid())
  companyId        String    @db.VarChar(36)
  agentId          String    @db.VarChar(36)
  agentVersionId   String    @db.VarChar(36)
  roleId           String    @db.VarChar(36)
  projectId        String?   @db.VarChar(36)
  offerId          String    @unique @db.VarChar(36)
  paymentOrderId   String?   @unique(map: "ai_direct_employments_payment_order_key") @db.VarChar(36)
  requestedByUserId String?  @db.VarChar(191)
  status           String    @default("candidate") @db.VarChar(32)
  startedAt        DateTime?
  endedAt          DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  @@index([companyId, status, updatedAt])
  @@index([agentId, status])
  @@index([agentVersionId])
  @@index([roleId])
  @@index([requestedByUserId, status])
  @@map("ai_direct_employments")
}

model aiDirectWorkforceEmployeeDigests {
  employmentId      String   @id @db.VarChar(36)
  organizationId    String   @db.VarChar(36)
  companyId         String   @db.VarChar(36)
  departmentId      String   @db.VarChar(36)
  positionId        String   @db.VarChar(36)
  roleId            String   @db.VarChar(36)
  agentId           String   @db.VarChar(36)
  agentVersionId    String   @db.VarChar(36)
  agentDisplayName  String   @db.VarChar(160)
  avatarAssetId     String?  @db.VarChar(36)
  departmentName    String   @db.VarChar(160)
  positionName      String   @db.VarChar(160)
  roleName          String   @db.VarChar(160)
  employmentStatus  String   @db.VarChar(32)
  startedAt         DateTime?
  revision          String   @db.Char(64)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([companyId, employmentStatus, updatedAt, employmentId], map: "workforce_employee_digests_company_cursor_idx")
  @@index([companyId, departmentId, updatedAt, employmentId], map: "workforce_employee_digests_company_department_cursor_idx")
  @@index([organizationId, companyId], map: "workforce_employee_digests_organization_company_idx")
  @@map("ai_direct_workforce_employee_digests")
}

model aiDirectEmploymentEvents {
  id            String    @id @default(cuid())
  employmentId  String    @db.VarChar(36)
  sequence      Int
  fromStatus    String?   @db.VarChar(32)
  toStatus      String    @db.VarChar(32)
  actorUserId   String    @db.VarChar(191)
  reason        String?   @db.Text
  approvalId    String?   @db.VarChar(36)
  metadata      Json?
  occurredAt    DateTime  @default(now())

  @@unique([employmentId, sequence])
  @@index([employmentId, occurredAt])
  @@map("ai_direct_employment_events")
}

model aiDirectApprovals {
  id                  String    @id @default(cuid())
  organizationId      String?   @db.VarChar(36)
  targetType          String    @db.VarChar(64)
  targetId            String    @db.VarChar(191)
  requestedByUserId   String    @db.VarChar(191)
  approverUserId      String?   @db.VarChar(191)
  status              String    @default("pending") @db.VarChar(32)
  decision            String?   @db.VarChar(32)
  decisionReason     String?   @db.Text
  expiresAt           DateTime?
  decidedAt           DateTime?
  metadata            Json?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  @@index([organizationId, status, updatedAt])
  @@index([status, expiresAt, id], map: "ai_direct_approvals_pending_expiry_idx")
  @@index([targetType, targetId, status])
  @@index([approverUserId, status, updatedAt])
  @@map("ai_direct_approvals")
}

model aiDirectApprovalEvents {
  id             String    @id @db.VarChar(36)
  approvalId     String    @db.VarChar(36)
  organizationId String?   @db.VarChar(36)
  sequence       Int
  eventType      String    @db.VarChar(64)
  actorUserId    String?   @db.VarChar(191)
  requestId      String    @db.VarChar(128)
  metadata       Json?
  occurredAt     DateTime  @default(now())

  @@unique([approvalId, sequence], map: "ai_direct_approval_events_approval_sequence_key")
  @@index([approvalId, occurredAt, id], map: "ai_direct_approval_events_approval_occurred_idx")
  @@index([organizationId, occurredAt, id], map: "ai_direct_approval_events_org_occurred_idx")
  @@map("ai_direct_approval_events")
}

model aiDirectApprovalDelegations {
  id                String   @id @db.VarChar(36)
  approvalId        String   @db.VarChar(36)
  organizationId    String   @db.VarChar(36)
  fromUserId        String?  @db.VarChar(191)
  toUserId          String   @db.VarChar(191)
  delegatedByUserId String   @db.VarChar(191)
  reason            String?  @db.VarChar(500)
  createdAt         DateTime @default(now())

  @@index([approvalId, createdAt, id], map: "ai_direct_approval_delegations_approval_created_idx")
  @@index([organizationId, toUserId, createdAt, id], map: "ai_direct_approval_delegations_org_to_created_idx")
  @@map("ai_direct_approval_delegations")
}

model aiDirectInterviewRetentionPolicies {
  organizationId      String   @id @db.VarChar(36)
  bodyRetentionDays   Int      @default(90)
  modelConsentMode    String   @default("organization_default_opt_in") @db.VarChar(64)
  attachmentPolicy    String   @default("image_pdf_only") @db.VarChar(64)
  attachmentMaxBytes  Int      @default(10485760)
  version             Int      @default(1)
  updatedByUserId     String   @db.VarChar(191)
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@map("ai_direct_interview_retention_policies")
}

model aiDirectInterviewConversations {
  id             String   @id @db.VarChar(36)
  organizationId String   @db.VarChar(36)
  agentVersionId String   @db.VarChar(36)
  createdByUserId String  @db.VarChar(191)
  status         String   @default("active") @db.VarChar(32)
  latestSequence Int      @default(0)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([organizationId, status, updatedAt])
  @@map("ai_direct_interview_conversations")
}

model aiDirectInterviewParticipants {
  id                 String    @id @db.VarChar(36)
  conversationId     String    @db.VarChar(36)
  userId             String    @db.VarChar(191)
  status             String    @default("active") @db.VarChar(32)
  modelUseOptedOutAt DateTime?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt

  @@unique([conversationId, userId])
  @@index([userId, status, updatedAt])
  @@map("ai_direct_interview_participants")
}

model aiDirectInterviewMessages {
  id                 String    @id @db.VarChar(36)
  conversationId     String    @db.VarChar(36)
  sequence           Int
  senderUserId       String    @db.VarChar(191)
  body               String    @db.Text
  retentionExpiresAt DateTime
  deletedAt          DateTime?
  deletedByUserId    String?   @db.VarChar(191)
  createdAt          DateTime  @default(now())

  @@unique([conversationId, sequence])
  @@index([conversationId, sequence])
  @@index([retentionExpiresAt, deletedAt])
  @@map("ai_direct_interview_messages")
}

model aiDirectInterviewReadCursors {
  conversationId String   @db.VarChar(36)
  userId         String   @db.VarChar(191)
  sequence       Int      @default(0)
  updatedAt      DateTime @updatedAt

  @@id([conversationId, userId])
  @@index([userId, updatedAt])
  @@map("ai_direct_interview_read_cursors")
}

model aiDirectInterviewLegalHolds {
  id             String    @id @db.VarChar(36)
  organizationId String    @db.VarChar(36)
  conversationId String?   @db.VarChar(36)
  messageId      String?   @db.VarChar(36)
  reason         String    @db.VarChar(500)
  status         String    @default("active") @db.VarChar(32)
  createdByUserId String   @db.VarChar(191)
  releasedByUserId String? @db.VarChar(191)
  releasedAt     DateTime?
  createdAt      DateTime  @default(now())

  @@index([organizationId, status])
  @@index([conversationId, status])
  @@index([messageId, status])
  @@map("ai_direct_interview_legal_holds")
}

model aiDirectInterviewAttachments {
  id                 String    @id @db.VarChar(36)
  messageId          String    @db.VarChar(36)
  storageKey         String    @unique @db.VarChar(512)
  mimeType           String    @db.VarChar(128)
  sizeBytes          BigInt
  sha256             String    @db.Char(64)
  retentionExpiresAt DateTime
  deletedAt          DateTime?
  createdAt          DateTime  @default(now())

  @@index([messageId])
  @@index([retentionExpiresAt, deletedAt])
  @@map("ai_direct_interview_attachments")
}

model aiDirectWorkflowRuns {
  id                      String    @id @default(cuid())
  organizationId          String?   @db.VarChar(36)
  employmentId            String?   @db.VarChar(36)
  agentVersionId          String?   @db.VarChar(36)
  workflowKey             String    @db.VarChar(128)
  workflowVersion         String?   @db.VarChar(64)
  status                  String    @default("queued") @db.VarChar(32)
  runAfter                DateTime?
  leaseOwner              String?   @db.VarChar(128)
  leaseExpiresAt          DateTime?
  lastHeartbeatAt         DateTime?
  requestedByUserId       String    @db.VarChar(191)
  idempotencyKey          String?   @db.VarChar(128)
  approvalId              String?   @db.VarChar(36)
  requestedModelPolicy    Json?
  resolvedModelCatalogId   String?   @db.VarChar(36)
  resolvedModelKey        String?   @db.VarChar(255)
  modelRunAuditIds        Json?
  inputSummary            Json?
  outputIndex             Json?
  tokenUsage              Json?
  costMicros              BigInt?
  latencyMs               Int?
  failureCode             String?   @db.VarChar(128)
  failureReason           String?   @db.Text
  metadata                Json?
  startedAt               DateTime?
  finishedAt              DateTime?
  createdAt               DateTime  @default(now())
  updatedAt               DateTime  @updatedAt

  @@index([organizationId, createdAt])
  @@index([organizationId, createdAt, id], map: "ai_direct_workflow_runs_organization_cursor_idx")
  @@index([employmentId, createdAt])
  @@index([status, updatedAt])
  @@index([requestedByUserId, status, updatedAt])
  @@unique([requestedByUserId, idempotencyKey])
  @@index([status, runAfter, leaseExpiresAt, createdAt], map: "ai_direct_workflow_runs_queue_idx")
  @@index([leaseOwner, leaseExpiresAt])
  @@map("ai_direct_workflow_runs")
}

model aiDirectWorkflowRunSteps {
  id               String    @id @default(cuid())
  runId            String    @db.VarChar(36)
  stepKey          String    @db.VarChar(128)
  sequence         Int
  status           String    @default("pending") @db.VarChar(32)
  attemptCount     Int       @default(0)
  maxAttempts      Int       @default(3)
  catalogModelId   String?   @db.VarChar(36)
  modelKey         String?   @db.VarChar(255)
  inputTokens      Int?
  outputTokens     Int?
  costMicros       BigInt?
  latencyMs        Int?
  failureCode      String?   @db.VarChar(128)
  lastFailureClass String?   @db.VarChar(64)
  startedAt        DateTime?
  finishedAt       DateTime?
  outputSummary    Json?
  metadata         Json?
  createdAt        DateTime  @default(now())

  @@unique([runId, sequence])
  @@index([runId, sequence])
  @@map("ai_direct_workflow_run_steps")
}

model aiDirectAgentAppearanceProfiles {
  agentId                String   @id @db.VarChar(36)
  avatarAssetId          String?  @db.VarChar(36)
  defaultMode            String   @default("image_2d") @db.VarChar(32)
  controllerEmploymentId String?  @unique(map: "appearance_profiles_controller_employment_key") @db.VarChar(36)
  controllerCompanyId    String?  @db.VarChar(36)
  revision               BigInt   @default(1)
  updatedByUserId        String   @db.VarChar(191)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
  agent                  aiDirectAgents @relation(fields: [agentId], references: [id], onDelete: Cascade)

  @@index([controllerCompanyId], map: "appearance_profiles_controller_company_idx")
  @@index([updatedAt], map: "appearance_profiles_updated_at_idx")
  @@map("ai_direct_agent_appearance_profiles")
}

model aiDirectAgentAppearanceAssets {
  id               String    @id @db.VarChar(36)
  agentId          String    @db.VarChar(36)
  kind             String    @db.VarChar(32)
  sortOrder        Int       @default(0)
  status           String    @default("active") @db.VarChar(32)
  storageKey       String    @unique(map: "appearance_assets_storage_key") @db.VarChar(512)
  originalFileName String    @db.VarChar(255)
  mimeType         String    @db.VarChar(128)
  sizeBytes        BigInt
  sha256           String    @db.Char(64)
  createdByUserId  String    @db.VarChar(191)
  createdAt        DateTime  @default(now())
  deletedByUserId  String?   @db.VarChar(191)
  deletedAt        DateTime?
  agent            aiDirectAgents @relation(fields: [agentId], references: [id], onDelete: Cascade)

  @@index([agentId, kind, status, sortOrder], map: "appearance_assets_agent_kind_status_order_idx")
  @@index([agentId, status, createdAt], map: "appearance_assets_agent_status_created_idx")
  @@map("ai_direct_agent_appearance_assets")
}

model desktopSidebarPreferences {
  userId    String   @id @db.VarChar(191)
  config    Json
  revision  BigInt   @default(1)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([updatedAt], map: "desktop_sidebar_preferences_updated_at_idx")
  @@map("desktop_sidebar_preferences")
}

model desktopSidebarAssets {
  id               String    @id @db.VarChar(36)
  userId           String    @db.VarChar(191)
  storageKey       String    @unique(map: "desktop_sidebar_assets_storage_key") @db.VarChar(512)
  originalFileName String    @db.VarChar(255)
  mimeType         String    @db.VarChar(128)
  sizeBytes        BigInt
  sha256           String    @db.Char(64)
  createdAt        DateTime  @default(now())
  deletedAt        DateTime?

  @@index([userId, deletedAt, createdAt], map: "desktop_sidebar_assets_user_deleted_created_idx")
  @@map("desktop_sidebar_assets")
}

model desktopTemplates {
  id              String   @id @db.VarChar(36)
  publisherId     String   @db.VarChar(191)
  slug            String   @db.VarChar(160)
  name            String   @db.VarChar(160)
  description     String   @db.Text
  status          String   @default("draft") @db.VarChar(32)
  catalogStatus   String   @default("unpublished") @db.VarChar(32)
  pricingMode     String   @default("free") @db.VarChar(32)
  priceMicros     BigInt?
  currency        String?  @db.Char(3)
  activeVersionId String?  @db.VarChar(36)
  createdByUserId String   @db.VarChar(191)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([publisherId, slug], map: "desktop_templates_publisher_slug_key")
  @@index([status, updatedAt], map: "desktop_templates_status_updated_idx")
  @@index([catalogStatus, updatedAt], map: "desktop_templates_catalog_updated_idx")
  @@index([publisherId, catalogStatus, updatedAt], map: "desktop_templates_publisher_catalog_updated_idx")
  @@index([publisherId, status, updatedAt], map: "desktop_templates_publisher_status_updated_idx")
  @@map("desktop_templates")
}

model desktopTemplateVersions {
  id                String    @id @db.VarChar(36)
  templateId        String    @db.VarChar(36)
  version           String    @db.VarChar(64)
  status            String    @default("draft") @db.VarChar(32)
  reviewStatus      String    @default("draft") @db.VarChar(32)
  publicationStatus String    @default("unpublished") @db.VarChar(32)
  reviewDecisionId  String?   @db.VarChar(36)
  submittedAt       DateTime?
  reviewedAt        DateTime?
  manifest          Json
  dataSchemaVersion Int       @default(1)
  storageKey        String    @unique(map: "desktop_template_versions_storage_key") @db.VarChar(512)
  originalFileName  String    @db.VarChar(255)
  mimeType          String    @db.VarChar(128)
  sizeBytes         BigInt
  sha256            String    @db.Char(64)
  createdByUserId   String    @db.VarChar(191)
  createdAt         DateTime  @default(now())
  publishedAt       DateTime?

  @@unique([templateId, version], map: "desktop_template_versions_template_version_key")
  @@index([templateId, status, createdAt], map: "desktop_template_versions_template_status_created_idx")
  @@index([reviewStatus, submittedAt, id], map: "desktop_template_versions_review_queue_idx")
  @@index([templateId, publicationStatus, createdAt], map: "desktop_template_versions_template_publication_created_idx")
  @@index([reviewDecisionId], map: "desktop_template_versions_decision_idx")
  @@map("desktop_template_versions")
}

model desktopTemplateReviewDecisions {
  id                String   @id @db.VarChar(36)
  templateVersionId String   @db.VarChar(36)
  decision          String   @db.VarChar(32)
  reason            String?  @db.Text
  actorUserId       String   @db.VarChar(191)
  requestId         String   @db.VarChar(128)
  createdAt         DateTime @default(now())

  @@index([templateVersionId, createdAt, id], map: "desktop_template_review_version_created_idx")
  @@index([actorUserId, createdAt], map: "desktop_template_review_actor_created_idx")
  @@map("desktop_template_review_decisions")
}

model desktopTemplateOutbox {
  id            String    @id @db.VarChar(36)
  topic         String    @db.VarChar(128)
  aggregateType String    @db.VarChar(64)
  aggregateId   String    @db.VarChar(191)
  payload       Json
  status        String    @default("pending") @db.VarChar(32)
  attempts      Int       @default(0)
  availableAt   DateTime  @default(now())
  createdAt     DateTime  @default(now())
  publishedAt   DateTime?

  @@index([status, availableAt, createdAt], map: "desktop_template_outbox_dispatch_idx")
  @@index([aggregateType, aggregateId, createdAt], map: "desktop_template_outbox_aggregate_idx")
  @@map("desktop_template_outbox")
}

model desktopTemplateDownloadEvents {
  id                String   @id @db.VarChar(36)
  templateId        String   @db.VarChar(36)
  templateVersionId String   @db.VarChar(36)
  userId            String   @db.VarChar(191)
  entitlementSource String   @db.VarChar(32)
  requestId         String   @db.VarChar(128)
  downloadedAt      DateTime @default(now())

  @@index([templateId, downloadedAt, id], map: "desktop_template_download_template_created_idx")
  @@index([templateVersionId, downloadedAt, id], map: "desktop_template_download_version_created_idx")
  @@index([userId, downloadedAt], map: "desktop_template_download_user_created_idx")
  @@map("desktop_template_download_events")
}

model desktopTemplateScreenshots {
  id                String   @id @db.VarChar(36)
  templateVersionId String   @db.VarChar(36)
  sortOrder         Int
  storageKey        String   @unique(map: "desktop_template_screenshots_storage_key") @db.VarChar(512)
  mimeType          String   @db.VarChar(128)
  sizeBytes         BigInt
  sha256            String   @db.Char(64)
  createdAt         DateTime @default(now())

  @@unique([templateVersionId, sortOrder], map: "desktop_template_screenshots_version_order_key")
  @@index([templateVersionId], map: "desktop_template_screenshots_version_idx")
  @@map("desktop_template_screenshots")
}

model desktopTemplateEntitlements {
  id              String    @id @db.VarChar(36)
  templateId      String    @db.VarChar(36)
  userId          String    @db.VarChar(191)
  source          String    @db.VarChar(32)
  reference       String?   @db.VarChar(191)
  status          String    @default("active") @db.VarChar(32)
  grantedByUserId String?   @db.VarChar(191)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  revokedAt       DateTime?

  @@unique([templateId, userId], map: "desktop_template_entitlements_template_user_key")
  @@index([userId, status, updatedAt], map: "desktop_template_entitlements_user_status_updated_idx")
  @@index([templateId, status], map: "desktop_template_entitlements_template_status_idx")
  @@map("desktop_template_entitlements")
}

model desktopTemplateAuditEvents {
  id             String   @id @db.VarChar(36)
  organizationId String?  @db.VarChar(36)
  actorUserId    String   @db.VarChar(191)
  action      String   @db.VarChar(128)
  targetType  String   @db.VarChar(64)
  targetId    String   @db.VarChar(191)
  requestId   String   @db.VarChar(128)
  outcome     String   @default("success") @db.VarChar(32)
  metadata    Json?
  createdAt   DateTime @default(now())

  @@index([targetType, targetId, createdAt], map: "desktop_template_audit_target_created_idx")
  @@index([actorUserId, createdAt], map: "desktop_template_audit_actor_created_idx")
  @@index([organizationId, createdAt, id], map: "desktop_template_audit_org_time_cursor_idx")
  @@index([organizationId, actorUserId, createdAt, id], map: "desktop_template_audit_org_actor_time_cursor_idx")
  @@index([organizationId, targetType, targetId, createdAt, id], map: "desktop_template_audit_org_resource_time_cursor_idx")
  @@index([organizationId, action, createdAt, id], map: "desktop_template_audit_org_action_time_cursor_idx")
  @@index([organizationId, requestId, createdAt, id], map: "desktop_template_audit_org_request_time_cursor_idx")
  @@map("desktop_template_audit_events")
}
